# Copy to .env and fill in. .env is gitignored; this file is not.
#
# Optional. The whole test suite forks Base at a pinned block. Leaving this unset (or empty)
# falls back to the public endpoint https://mainnet.base.org, which works but rate limits.
# Any archive-capable Base endpoint will do.
BASE_RPC_URL=
